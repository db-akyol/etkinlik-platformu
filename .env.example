NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
ANTHROPIC_API_KEY=

# Optional. Only needed once the site has a custom domain — on Vercel it
# otherwise falls back to VERCEL_PROJECT_PRODUCTION_URL automatically.
# Used for canonical links, Open Graph URLs, sitemap.xml and robots.txt.
NEXT_PUBLIC_SITE_URL=
